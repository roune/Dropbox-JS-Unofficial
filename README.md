# Dropbox-JS-Unofficial
Unofficial Javascript API for dropbox. It works with their HTTP V2 CORE. Just sends ajax requests properly and passes the info via callbacks. It can be used directly on client-side.
